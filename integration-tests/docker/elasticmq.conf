# the include should be done only once, at the beginning of the custom configuration file
include classpath("application.conf")

queues {
  test-tws-control-standard {
    defaultVisibilityTimeout = 30 seconds
    deadLettersQueue {
      name = "test-tws-control-standard-dlq"
      maxReceiveCount = 1
    }
  }
  test-tws-control-premium {
    defaultVisibilityTimeout = 30 seconds
    deadLettersQueue {
      name = "test-tws-control-premium-dlq"
      maxReceiveCount = 1
    }
  }
  test-tws-compute-standard {
    defaultVisibilityTimeout = 30 seconds
    deadLettersQueue {
      name = "test-tws-compute-standard-dlq"
      maxReceiveCount = 1
    }
  }
  test-tws-compute-premium {
    defaultVisibilityTimeout = 30 seconds
    deadLettersQueue {
      name = "test-tws-compute-premium-dlq"
      maxReceiveCount = 1
    }
  }
  test-tws-control-standard-dlq {}
  test-tws-control-premium-dlq {}
  test-tws-compute-standard-dlq {}
  test-tws-compute-premium-dlq {}
}
